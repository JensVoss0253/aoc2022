function part1(filename)
    f = open(filename)
    lines = readlines(f)
    score = 0
    score
end

function part2(filename)
    f = open(filename)
    lines = readlines(f)
    score = 0
    score
end